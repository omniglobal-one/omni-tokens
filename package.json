{
  "name": "@omni/tokens",
  "version": "1.0.1",
  "description": "The OMNI Standard, Level 1 — shared design tokens: neutrals, semantic colors, type scale, spacing/radius/elevation rules, self-hosted fonts, and the six-token product-accent pattern. Every OMNI product repo consumes this package; none of them fork it.",
  "license": "UNLICENSED",
  "private": true,
  "main": "src/index.js",
  "types": "src/index.d.ts",
  "files": [
    "src",
    "fonts",
    "README.md"
  ],
  "exports": {
    ".": "./src/index.js",
    "./tailwind-preset": "./src/tailwind-preset.js",
    "./tokens.css": "./src/tokens.css",
    "./fonts.css": "./src/fonts.css",
    "./fonts/*": "./fonts/*"
  }
}
